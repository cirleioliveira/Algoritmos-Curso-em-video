algoritmo "Soma"
var
   n1, n2, soma: inteiro
inicio
      escreval("Informe um numero:")
      leia(n1)
      escreval("Informe um numero:")
      leia(n2)
      
      soma <- n1+n2
      
      escreva ("A soma de ", n1, " e ", n2, " é = ", soma)
fimalgoritmo
